# Εξάσκηση από το κινητό

Το GitHub από μόνο του σού δίνει μόνο **ανάγνωση** των μαθημάτων. Για να *τρέχεις* ερωτήματα
από το κινητό υπάρχουν τρεις δρόμοι — ο πρώτος είναι ο καλύτερος για καθημερινή εξάσκηση.

---

## 1. Κονσόλα SQL μέσα στον browser (χωρίς λογαριασμό, χωρίς όρια)

Ο φάκελος [`web/`](web/) περιέχει μια σελίδα που τρέχει **ολόκληρη τη βάση μέσα στο κινητό**,
με SQLite μεταγλωττισμένη σε WebAssembly. Καμία σύνδεση με server, κανένα κόστος, δουλεύει
και με κλειστά δεδομένα αφού φορτώσει μία φορά.

**Ενεργοποίηση (μία φορά, από υπολογιστή ή κινητό):**

1. GitHub → το repo → **Settings** → **Pages**
2. *Source*: **Deploy from a branch**
3. *Branch*: `claude/sql-lesson-q70aj1` (ή `main` αν το κάνεις merge), φάκελος `/ (root)` → **Save**
4. Μετά από ~1 λεπτό η σελίδα είναι στο:
   `https://stergios-ziliaskopoulos.github.io/SQL/web/`

**Πρόσθεσέ τη στην αρχική οθόνη του κινητού** (Safari: Μοιραστείτε → Στην αφετηρία /
Chrome: ⋮ → Προσθήκη στην αρχική οθόνη) και ανοίγει σαν εφαρμογή.

Τι έχει:
- πεδίο για ερωτήματα (χωρίς αυτόματα κεφαλαία/διόρθωση — σημαντικό στο κινητό)
- έτοιμα παραδείγματα ανά μάθημα, για να μη γράφεις μεγάλα ερωτήματα με τα δάχτυλα
- κουμπί «Πίνακες» και πτυσσόμενο σχήμα της βάσης, για να θυμάσαι τα ονόματα στηλών
- «Επαναφορά βάσης» — χάλασέ την όσο θέλεις

---

## 2. GitHub Codespaces — πλήρες περιβάλλον με terminal

Όταν θέλεις να τρέξεις το `sql.py`, να γράψεις αρχεία και να κάνεις commit από το κινητό.

1. Άνοιξε το repo στον browser του κινητού (όχι στην εφαρμογή GitHub)
2. Κουμπί **Code** → καρτέλα **Codespaces** → **Create codespace**
3. Ανοίγει VS Code μέσα στον browser, με terminal:

```bash
python3 tools/build_db.py
python3 sql.py
```

Το [`.devcontainer/devcontainer.json`](.devcontainer/devcontainer.json) στήνει Python και
χτίζει τη βάση αυτόματα. Ο δωρεάν λογαριασμός δίνει ~60 ώρες/μήνα — αρκετές, αλλά
**σβήνε το codespace** όταν τελειώνεις (Code → Codespaces → ⋯ → Delete).

Στο κινητό το VS Code είναι στριμωγμένο· καλό για γρήγορη αλλαγή και commit, όχι για ώρες μελέτης.

---

## 3. Ανάγνωση μαθημάτων — η εφαρμογή GitHub

Η επίσημη εφαρμογή GitHub (iOS/Android) εμφανίζει τα `.md` μορφοποιημένα. Ιδανική για να
διαβάζεις ένα μάθημα στη σειρά ή στο λεωφορείο. Δεν εκτελεί τίποτα — συνδύασέ τη με το (1).

---

## Πρακτικός συνδυασμός

| Πότε | Τι χρησιμοποιείς |
|---|---|
| Διάβασμα θεωρίας | εφαρμογή GitHub |
| 10λεπτη εξάσκηση με ερωτήματα | σελίδα `web/` στην αρχική οθόνη |
| Σοβαρή δουλειά, αλλαγές, commit | υπολογιστής ή Codespaces |
